# Remember that the path is relative to the folder where the test are execute.
# By default it is the main folder of the repository.
tf-probabilities-path: test/connect4/files/TF-testModel-Probabilities
